import type { Metadata } from "next";
import { ArticleShell, Callout, Code, H2, P } from "../shell";

export const metadata: Metadata = {
  title: "5,000만 행 시계열을 일상 조회가 가능한 상태로 유지하기",
  description:
    "인스타그램 계정 4,800여 개의 메트릭 스냅샷이 5,000만 행으로 자라는 동안 내린 스키마·인덱스·수집 파이프라인 판단을 기록합니다.",
};

export default function Page() {
  return (
    <ArticleShell
      label="DATA AT SCALE · FORCLETTER"
      title="5,000만 행 시계열을 일상 조회가 가능한 상태로 유지하기"
      date="2026.08"
      intro="포크레터는 인스타그램 계정 4,800여 개의 메트릭을 주기적으로 수집합니다. 이 글은 스냅샷 테이블이 5,000만 행으로 자라는 동안 내린 판단들 — 왜 스냅샷인가, 인덱스를 어떻게 걸었나, JSON 컬럼을 왜 버렸나 — 를 기록한 글입니다."
    >
      <H2>왜 원본을 다시 조회하지 않고 스냅샷으로 쌓는가</H2>
      <P>
        인스타그램 Graph API는 과거 시점의 지표를 돌려주지 않습니다. 오늘
        조회하면 오늘의 팔로워 수와 오늘까지의 누적 조회수를 줄 뿐, &quot;지난달
        15일의 값&quot;은 어디에도 없습니다. 크리에이터에게 성장 추이를
        보여주려면 방법은 하나뿐입니다 — 매 주기 값을 우리 쪽에 박제(스냅샷)해
        두는 것.
      </P>
      <P>
        문제는 산수입니다. 계정 4,800여 개, 계정마다 수십~수백 개의 미디어,
        미디어마다 주기적인 메트릭 수집. 이 곱셈은 금방 커져서 현재 미디어
        메트릭 스냅샷 테이블은 약 4,900만 행, 계정 정보 스냅샷은 약 510만
        행입니다. 그리고 이 테이블 위에서 성장·활성·리텐션 통계와 주간
        리포트가 매일 돌아야 합니다.
      </P>

      <H2>append-only 테이블과 두 개의 인덱스</H2>
      <P>
        스냅샷 테이블은 수정하지 않습니다. 수집 시점마다 새 행을 추가하는
        append-only 구조라 쓰기 경합이 없고, 과거가 불변이라 집계 결과도
        재현됩니다. 대신 조회가 문제인데, 실제 조회 패턴을 뜯어보면 두
        가지뿐이었습니다.
      </P>
      <Code>{`-- 패턴 1: 특정 미디어의 기간 추이 (미디어 상세, 리포트)
SELECT ... WHERE mediaId = ? AND periodEnd BETWEEN ? AND ?

-- 패턴 2: 기간 전체를 훑는 집계 (대시보드 통계, 정리 배치)
SELECT ... WHERE periodEnd >= ? AND periodEnd < ?`}</Code>
      <P>
        그래서 인덱스도 정확히 두 개만 걸었습니다. 복합 인덱스{" "}
        <strong>(mediaId, periodEnd)</strong>가 패턴 1을, 단독 인덱스{" "}
        <strong>(periodEnd)</strong>가 패턴 2를 받칩니다. 5,000만 행에서
        인덱스를 &quot;혹시 몰라서&quot; 추가하면 쓰기 비용과 스토리지가 매
        수집 주기마다 청구됩니다. 조회 패턴에 없는 인덱스는 걸지 않는다 — 가
        이 테이블의 규칙입니다.
      </P>

      <H2>JSON 컬럼을 버리고 백필한 이유</H2>
      <P>
        초기 스키마는 메트릭 응답을 JSON 컬럼에 그대로 넣었습니다. API 응답이
        바뀌어도 스키마 변경이 없으니 초기엔 합리적인 선택이었지만, 통계
        쿼리가 늘면서 한계가 왔습니다. JSON 추출 함수 위에는 인덱스가 없고,
        집계마다 수천만 행의 JSON 파싱 비용을 내야 했으니까요.
      </P>
      <P>
        그래서 자주 조회하는 지표를 정규 컬럼으로 승격하고, 기존 행은 SQL
        백필 스크립트로 JSON에서 컬럼으로 옮겼습니다. 스키마 유연성이 필요한
        구간(초기)과 조회 성능이 필요한 구간(운영)은 다르며, 그 전환 비용을
        치를 타이밍을 놓치지 않는 게 중요하다는 걸 배웠습니다.
      </P>

      <H2>수집 파이프라인: 크론 35개와 중복 실행 락</H2>
      <P>
        수집은 35개의 크론 잡이 나눠 맡습니다. 여기서 겪은 함정 하나 — 크론은
        이전 실행이 끝났는지 관심이 없습니다. API가 느려져 수집이 주기를
        넘기면 같은 잡이 겹쳐 돌고, 스냅샷이 중복으로 쌓입니다. 그래서 모든
        잡을 락으로 감쌌습니다.
      </P>
      <Code>{`async function withLock(jobName: string, fn: () => Promise<void>) {
  if (runningJobs.has(jobName)) {
    logger.warn(\`Skipping \${jobName}: previous run still in progress\`);
    return; // 겹치면 건너뛴다 — 다음 주기가 곧 온다
  }
  runningJobs.add(jobName);
  try { await fn(); }
  finally { runningJobs.delete(jobName); }
}`}</Code>
      <P>
        실패는 Slack으로 알리되 같은 잡의 반복 실패는 30분 쿨다운으로
        묶습니다. 새벽에 API가 흔들리면 알림 수백 개가 오는 게 아니라, &quot;이
        잡이 실패 중&quot;이라는 신호 하나가 옵니다.
      </P>

      <Callout>
        요약 — ① 과거를 주지 않는 외부 API 위에서는 스냅샷이 유일한 시계열이다.
        ② 인덱스는 실제 조회 패턴의 수만큼만. ③ JSON의 유연함은 초기 비용
        절감이고, 운영에 들어가면 컬럼으로 갚는다. ④ 크론에는 반드시 중복 실행
        락을.
      </Callout>

      <H2>남은 고민</H2>
      <P>
        다음 병목은 예측 가능합니다. 행이 계속 자라면 (periodEnd) 범위 스캔의
        비용도 자랍니다. 기간 파티셔닝과 오래된 스냅샷의 집계-후-아카이빙을
        검토 중이고, 그 전환도 이번 백필처럼 &quot;필요해지기 반 박자 전&quot;에
        하는 것이 목표입니다.
      </P>
    </ArticleShell>
  );
}
