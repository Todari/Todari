import type { Metadata } from "next";
import { ArticleShell, Callout, Code, H2, P } from "../shell";

export const metadata: Metadata = {
  title: "토큰 4,800개의 수명주기 — 상태 머신과 서킷 브레이커",
  description:
    "인스타그램 계정 4,800여 개의 액세스 토큰을 운영하며 배운 것 — 토큰 값이 아니라 상태가 진실이고, 외부 장애가 사용자의 의도를 덮어쓰면 안 된다.",
};

export default function Page() {
  return (
    <ArticleShell
      label="EXTERNAL API AT SCALE · FORCLETTER"
      title="토큰 4,800개의 수명주기 — 상태 머신과 서킷 브레이커"
      date="2026.08"
      intro="포크레터는 인스타그램 계정 4,800여 개의 액세스 토큰 위에서 돌아갑니다. 토큰은 수시로 만료되고, 사용자는 연동을 끊고, Meta API는 가끔 통째로 흔들립니다. 이 글은 그 혼돈을 관리하기 위해 세운 한 가지 원칙 — '토큰 값이 아니라 상태가 진실이다' — 에 대한 이야기입니다."
    >
      <H2>토큰 값은 진실이 아니다</H2>
      <P>
        초기의 순진한 모델은 &quot;토큰이 있으면 연동된 계정&quot;이었습니다.
        이 모델은 규모가 커지자 바로 무너집니다. 토큰이 있어도 만료됐을 수
        있고, 유효해도 사용자가 탈퇴 의사로 연동을 끊었을 수 있고, 계정이
        비즈니스에서 개인 계정으로 바뀌어 API 자체가 막혔을 수도 있습니다.
        그래서 토큰 문자열과 별개로 <code>tokenStatus</code>라는 명시적 상태를
        운영의 단일 진실로 삼았습니다.
      </P>
      <Code>{`tokenStatus
├─ active            정상 — 수집·자동화 대상
├─ expired           만료 — 재연동 안내 대상
├─ disconnected      사용자가 의도적으로 끊음
│                    → API 호출 금지, 재연동 안내도 금지
└─ personal_account  개인 계정 전환 — API 불가, 재시도 무의미`}</Code>
      <P>
        핵심 규칙은 상태마다 &quot;시스템이 해도 되는 일&quot;이 정의된다는
        것입니다. <code>disconnected</code>는 단순한 에러 상태가 아니라
        사용자의 의사 표시라서, 이 상태의 계정에는 API 호출도, &quot;다시
        연결하세요&quot; 알림도 보내지 않습니다.
      </P>

      <H2>가장 중요한 규칙 — 에러는 의도를 덮어쓸 수 없다</H2>
      <P>
        이 설계에서 제일 값비싸게 배운 규칙입니다. Meta API가 흔들리는 날엔
        멀쩡한 토큰도 401·타임아웃을 뱉습니다. 그때 에러 핸들러가 순진하게
        상태를 <code>expired</code>로 바꿔버리면, 외부 장애 한 번에 수천
        계정이 &quot;만료&quot;로 오염되고 사용자 수천 명에게 잘못된 재연동
        안내가 나갑니다. 반대 방향도 같습니다 — API 에러가{" "}
        <code>disconnected</code>를 덮어쓰면, 떠난 사용자를 시스템이 다시
        건드리게 됩니다.
      </P>
      <P>
        그래서 상태 전이에 방향 규칙을 뒀습니다. 일시적 실패는 상태를 바꾸지
        못하고, <code>expired</code> 판정은 명확한 영구 오류 코드에서만,
        사용자 의도 상태(<code>disconnected</code>)는 오직 사용자 행동으로만
        바뀝니다. 상태 머신의 가치는 상태의 개수가 아니라{" "}
        <strong>금지된 전이</strong>에 있습니다.
      </P>

      <H2>서킷 브레이커 — 계정이 아니라 시스템을 지킨다</H2>
      <P>
        4,800개 계정을 35개 크론이 순회하는 구조에서 Meta가 전체적으로
        흔들리면, 재시도가 재시도를 부르며 우리 쪽 워커와 상대의 레이트리밋을
        같이 태웁니다. 그래서 토큰 처리 경로에 서킷 브레이커를 걸었습니다 —
        실패가 임계를 넘으면 해당 경로를 잠시 끊고, 끊겼다는 사실 자체를
        치명 등급 경보로 올립니다. 개별 계정의 실패는 로그지만, 서킷이 열린
        건 사건입니다. 경보 등급을 이 둘로 나누는 것이 &quot;알림이 많아서
        아무도 안 보는&quot; 운영을 막아 줍니다.
      </P>

      <Callout>
        요약 — ① 외부 자격증명은 값이 아니라 명시적 상태로 관리한다. ② 상태
        머신의 본질은 금지된 전이다: 외부 장애는 사용자 의도를 절대 덮어쓸 수
        없다. ③ 대량 연동에서는 개별 실패(로그)와 경로 차단(사건)의 경보
        등급을 분리한다.
      </Callout>

      <H2>이 원칙이 산 곳</H2>
      <P>
        이 규칙들은 문서로도 박제해 뒀습니다 — 새 기능이 토큰을 만질 때마다
        코드 리뷰에서 같은 논쟁을 반복하지 않기 위해서입니다. 1인 개발이라도,
        아니 1인 개발이라서 더, 미래의 나와 AI 에이전트가 따라야 할 운영
        정책은 코드 바깥의 문장으로 존재해야 한다고 생각합니다.
      </P>
    </ArticleShell>
  );
}
