import type { Metadata } from "next";
import { ArticleShell, Callout, Code, H2, P } from "../shell";

export const metadata: Metadata = {
  title: "새벽 3시의 OOM을 시스템으로 되돌리기",
  description:
    "서비스 10여 개가 올라간 단일 EC2가 새벽에 메모리 포화로 응답 불능이 된 사고 — 복구, 원인 제거, 그리고 같은 장애가 사람 없이 끝나게 만든 과정.",
};

export default function Page() {
  return (
    <ArticleShell
      label="INCIDENT RESPONSE · TODARI OPS"
      title="새벽 3시의 OOM을 시스템으로 되돌리기"
      date="2026.08"
      intro="7월 어느 새벽 3시 35분, 서비스 10여 개가 올라간 EC2가 응답 불능이 됐습니다. 저는 자고 있었고, 아침에야 알았습니다. 이 글은 그 사고를 복구한 기록이 아니라 — 복구는 재부팅 한 줄이었으니까 — 같은 장애가 다시 오면 사람 없이 끝나도록 시스템을 바꾼 기록입니다."
    >
      <H2>무슨 일이 있었나</H2>
      <P>
        메트로놈들·행동대장 API·운영 봇 등 컨테이너 10여 개를 4GB 메모리의
        인스턴스 한 대에 올려 쓰고 있었습니다. 그날 새벽 node 프로세스 하나가
        메모리를 밀어 올렸고, OOM 킬러가 프로세스들을 반복적으로 죽이다가
        인스턴스 자체가 상태 검사 실패(impaired)에 빠졌습니다. 원인은
        단순했습니다 — 메모리는 빠듯한데 <strong>스왑이 없었습니다.</strong>{" "}
        완충 지대가 없으니 순간 피크가 곧장 시스템 전체의 죽음으로
        이어졌습니다.
      </P>
      <P>
        복구 자체는 싱거웠습니다. 원격 재부팅 한 번, 컨테이너들은{" "}
        <code>restart: unless-stopped</code> 정책 덕에 스스로 돌아왔습니다.
        여기서 끝냈다면 &quot;운이 나빴다&quot;로 남았을 겁니다. 하지만 이
        장애의 진짜 문제는 OOM이 아니라, <strong>몇 시간 동안 아무도
        몰랐다는 것</strong>이었습니다.
      </P>

      <H2>원인 제거 — 스왑이라는 완충 지대</H2>
      <Code>{`# 4GB 스왑파일 추가 + 재부팅에도 유지되도록 fstab 등록
sudo fallocate -l 4G /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile

# 스왑은 '비상구'로만 — 평소엔 램을 쓰도록 성향을 낮춘다
echo "vm.swappiness=10" | sudo tee /etc/sysctl.d/99-swappiness.conf`}</Code>
      <P>
        스왑은 성능 해결책이 아니라 관측 시간을 버는 장치입니다. 메모리 피크가
        와도 즉사 대신 &quot;느려짐&quot;으로 강등되고, 그 사이에 경보가 사람을
        부를 수 있습니다. swappiness를 10으로 낮춘 건 그 반대급부 — 스왑이
        일상이 되면 그것대로 성능 장애이기 때문입니다.
      </P>

      <H2>감지 — 죽기 전에 우는 경보</H2>
      <P>
        운영 봇(공개 코드)에 리소스 모니터를 붙였습니다. 5분마다{" "}
        <code>/proc/meminfo</code>를 읽어 메모리 90%, 스왑 60% 임계를 넘으면
        Discord로 경고합니다. 여기서 배운 디테일 하나 — 임계 경보에는{" "}
        <strong>히스테리시스</strong>가 필요합니다. 89%↔91%를 오가는 밤에 경보
        수십 개가 쌓이면, 사람은 경보를 끕니다. 경계를 넘을 때 한 번만 울고,
        충분히 내려와야 재무장하게 했습니다.
      </P>

      <H2>복구의 자동화 — 사람이 마지막 수단이 되도록</H2>
      <P>
        그래도 죽는 날은 옵니다. 그래서 복구를 3단으로 쌓았습니다.
      </P>
      <Code>{`1단  CloudWatch: StatusCheckFailed_Instance 3분 지속 → 자동 재부팅
2단  CloudWatch: StatusCheckFailed_System 2분 지속 → 인스턴스 복구
3단  운영 봇: 부팅 후 uptime < 15분이면 "재부팅 감지" 통지
     → 별도 알림 인프라 없이, 봇이 살아 돌아온 것 자체가 통지가 된다`}</Code>
      <P>
        3단이 이 구조에서 제일 마음에 드는 부분입니다. &quot;박스가 죽으면
        어떻게 알리지?&quot;라는 문제는 죽은 박스가 알림을 못 보낸다는 모순을
        안고 있는데, 순서를 뒤집으면 풀립니다 — CloudWatch가 박스를 살리고,
        살아난 봇이 자기 uptime을 보고 &quot;방금 재부팅됐어요&quot;라고
        말하게 하는 것. SNS도 Lambda도 필요 없었습니다.
      </P>

      <Callout>
        요약 — ① 장애 대응의 성과물은 복구가 아니라 재발 시나리오다. ② 스왑은
        성능 장치가 아니라 경보가 사람을 부를 시간을 버는 장치다. ③ 임계
        경보에는 히스테리시스를. ④ &quot;죽은 서버의 알림&quot; 문제는 자동
        복구 + 생존 신고로 뒤집어 푼다.
      </Callout>

      <H2>남은 정직한 사실</H2>
      <P>
        이 구조는 4GB 한 대에 서비스를 몰아넣은 비용 최적화의 대가이기도
        합니다. 서비스별 메모리 상한, 인스턴스 분리 같은 더 근본적인 처방도
        알고 있고, 트래픽이 그 비용을 정당화하는 시점에 집행할 계획입니다.
        중요한 건 그 시점이 오기 전까지 — 같은 장애가 와도 이제는 몇 분 안에,
        사람 없이 끝난다는 것입니다.
      </P>
    </ArticleShell>
  );
}
